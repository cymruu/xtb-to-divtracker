# xtb-to-divtracker

A simple web application that allows users to convert a CSV file exported from XTB into a format compatible with the DivTracker mobile app, making it easy to track dividend progress.

**All data processing happens locally on the user's device, ensuring that no sensitive data is sent over the internet.**
